{
    "name": "generate-corona-risk-areas",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "axios": "^0.20.0",
        "cheerio": "^1.0.0-rc.3",
        "csvtojson": "^2.0.10",
        "moment": "^2.27.0",
        "moment-timezone": "^0.5.31"
    },
    "devDependencies": {
        "typescript": "^4.0.2",
        "@types/node": "^12.12.7",
        "jest": "^24.7.1"
    },
    "scripts": {
        "test": "jest",
        "build": "tsc && cp -r data dist",
        "scraper": "npm run build && node dist/index.js"
    }
}
