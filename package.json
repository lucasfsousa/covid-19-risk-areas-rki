{
  "name": "vector-layer",
  "dependencies": {
    "ol": "6.4.3"
  },
  "devDependencies": {
    "parcel": "1.11.0"
  },
  "scripts": {
    "start": "node_modules/.bin/parcel index.html && cp countries.geojson dist",
    "build": "cd generate && npm run scraper && cd ../ && node_modules/.bin/parcel build index.html --no-content-hash --no-source-maps --no-minify -d docs --public-url ./ && cp countries.geojson docs"
  }
}
